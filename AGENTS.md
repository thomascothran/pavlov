# Rules

## Running tests

### Clojurescript
Use the bash tool to run `npx shadow-cljs compile test`

### Clojure
Use `kaocha.repl/run` at the REPL. This can be used at the test, test namespace, or test suite level.

See the clojure-eval skill for how to use the REPL
